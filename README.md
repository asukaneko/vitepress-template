# VitePress 模板项目

这是一个基于 [VitePress](https://vitepress.dev/) 的通用文档模板，内置了多种功能插件，适用于个人博客、项目文档或知识库建设。

## 💡 特性

- ⚡️ **快速启动**：基于 Vite，毫秒级热更新。
- 🎨 **精美主题**：内置自定义样式和组件（如 `ArticleMetadata`）。
- 🔍 **本地搜索**：内置全文搜索功能。
- 📝 **数学公式**：支持 MathJax3。
- 🕒 **Git 历史**：自动显示页面最后更新时间和 Git 变更日志。
- 💬 **评论系统**：支持 Giscus 评论。
- 🐳 **部署友好**：内置 Nginx 配置，支持 Docker 部署。

## 🛠️ 快速开始

### 安装与运行

> **注意**：如果您是将本项目目录复制到新的地方，请务必执行以下步骤：
> 1. 删除现有的 `node_modules` 文件夹和 `package-lock.json` 文件。
> 2. 重新运行 `npm install`。

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run docs:dev

# 构建静态站点
npm run docs:build

# 预览构建结果
npm run docs:preview
```

### 服务管理

模板提供了一些便捷的脚本用于后台管理：

```bash
# 后台启动服务（日志记录在 docs.log）
npm run start

# 停止后台运行的开发服务器
npm run stop
```

## 📂 项目结构

```text
.
├── docs/                 # 文档源码目录
│   ├── .vitepress/       # VitePress 配置、主题和插件
│   │   ├── config.mts    # 核心配置文件
│   │   └── theme/        # 自定义主题和组件
│   ├── guide/            # 使用指南
│   └── index.md          # 首页内容
├── nginx.conf            # Nginx 部署配置
├── package.json          # 项目依赖和脚本
└── README.md             # 项目说明文档
```

## 🔌 内置插件

本项目预装了以下优质插件：

- `@nolebase/vitepress-plugin-enhanced-readabilities`: 增强阅读体验
- `@nolebase/vitepress-plugin-git-changelog`: 显示 Git 变更记录
- `@nolebase/vitepress-plugin-page-properties`: 页面属性展示
- `vitepress-plugin-comment-with-giscus`: Giscus 评论支持
- `markdown-it-mathjax3`: 数学公式渲染

## 📄 开源协议

本项目基于 [ISC License](LICENSE) 开源。
